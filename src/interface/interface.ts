export interface Imageurl {
  url: string;
  used: boolean;
}
export interface ItemList {
  id: string;
  pictureUrl: string;
  itemName: string;
  title: string;
  subtitle: string;
  itemDescription: string;
}
export interface rowtype {
  id: number;
  city: string;
  district: string;
  address: string;
}
