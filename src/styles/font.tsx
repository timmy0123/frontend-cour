import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    h1: {
      fontSize: "2rem",
      fontWeight: 700,
      color: "white",
    },
    h2: {
      fontSize: "1.5rem",
      fontWeight: 700,
      color: "white",
    },
  },
});

export default theme;
